# Bio.Data.SCOPData was removed in Biopython 1.82+, and this repo still imports it.
biopython>=1.81,<1.82
numpy>=1.26,<2
ProDy>=2.4
scipy>=1.12,<2
# Let torch pull the matching CUDA/triton dependencies for the target environment.
torch==2.2.1
ml-collections==0.1.1
dm-tree==0.1.8
